/**
 * Tests for desugarListPattern
 */

import { describe, expect, it } from "vitest";

import { desugarListPattern } from "./desugarListPattern.js";

describe("desugarListPattern", () => {
    it("should exist", () => {
        expect(desugarListPattern).toBeDefined();
    });
});
