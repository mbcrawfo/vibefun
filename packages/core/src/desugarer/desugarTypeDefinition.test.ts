/**
 * Tests for desugarTypeDefinition
 */

import { describe, expect, it } from "vitest";

import { desugarTypeDefinition } from "./desugarTypeDefinition.js";

describe("desugarTypeDefinition", () => {
    it("should exist", () => {
        expect(desugarTypeDefinition).toBeDefined();
    });
});
