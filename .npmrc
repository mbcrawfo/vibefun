# Dependency resolution
save-exact=true
auto-install-peers=true
strict-peer-dependencies=true

# Workspace
save-workspace-protocol=rolling
link-workspace-packages=true

# Security - pnpm v10 blocks scripts by default;
# ignore-scripts is redundant but kept for explicitness
ignore-scripts=true

# Publishing
provenance=true
publish-branch=main
