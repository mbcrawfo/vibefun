# Fast-check Bug Backlog

Tracks bugs surfaced by property-based tests during the fast-check rollout. See
`.claude/CODING_STANDARDS.md` "Property-based testing" section for the triage
flow (Tier 1 fix-now / Tier 2 fix-this-PR / Tier 3 halt-and-escalate).

When a property fails:
1. Reproduce locally with the printed seed.
2. Classify per the tiers below.
3. For Tier 2/3, add an entry here AND `it.skip("[BUG: VF-FC-####] ...")` in
   the test file with a comment linking back to this entry.
4. Closed entries move to commit history; only **open** bugs live here.

## Open

| ID | File | Property | Tier | Counterexample (shrunken) | Seed | Conjecture | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| VF-FC-0001 | `packages/core/src/types/test-arbitraries/optimizable-expr-arb.test.ts` | `optimizableExprArb` typechecks without unregistered throws | 2 | `CoreLambda { param: CoreVariantPattern { constructor: "A", args: [] }, body: CoreLet { pattern: CoreVariantPattern, value: CoreUnitLit, body: CoreRecord { fields: [] } } }` | 1447445058 | `lambdaArb` in `core-ast-arb.ts` uses the full `CorePattern` arb for the lambda parameter, but the typechecker requires `CoreVarPattern` or `CoreWildcardPattern` (post-desugaring invariant). Fix: restrict the param arb in `lambdaArb`, then re-enable the property. | Open |
| VF-FC-0002 | `packages/core/src/optimizer/` (suspected; codegen-observable) | n/a — discovered while authoring fixed-input spec-validation tests for evaluation order (chunk 09, F-52); not surfaced by a property | 3 | <pre>let mut counter = ref(0);<br>let tick = () => {<br>  let _x = 1;<br>  counter := !counter + 1;<br>  true;<br>};<br>tick();<br>// !counter is 0, expected 1</pre> | n/a | Compiled JS (via `compile - --emit js`) is `const tick = (_unused0) => (() => { const _x = 1; return true; })();` — the inner `let _ = (counter := …) in true` (wildcard-let from block desugaring) is collapsed to just `true`, dropping the side-effecting `:=`. Reordering to put the `:=` *before* the let-binding makes the assignment survive (this is why the existing AND/OR short-circuit tests at `tests/e2e/spec-validation/04-expressions.test.ts:177–204` work). None of the four optimizer passes I scanned (`dead-code-elim`, `inline`, `beta-reduction`, `eta-reduction`) obviously folds wildcard-let → body — `eliminateDeadLet` explicitly gates on `pattern.kind === "CoreVarPattern"` so wildcards are skipped. Suspected: a fixed-point interaction across multiple passes, or a pass not yet inspected (`constant-folding`, `cse`, `pattern-match-opt`). Worth dumping per-pass output via the optimizer's iteration loop to identify the collapsing pass. **Halt the PR for human triage** — this elides observable side effects and likely affects more than the F-52 test. F-52 was worked around by reordering tick's body (assignment before the let); a comment in `04-expressions.test.ts` notes this. | Open |
| VF-FC-0003 | `packages/core/src/typechecker/infer/infer-bindings.ts` (suspected; value-restriction logic in `computeBindingScheme`) | n/a — discovered while authoring fixed-input spec-validation tests for empty-list value restriction (chunk 10, 04b F-07); not surfaced by a property | 3 | <pre>let xs = [];<br>let useInt = (ys: List&lt;Int&gt;) =&gt; 0;<br>let useStr = (ys: List&lt;String&gt;) =&gt; 0;<br>let _ = useInt(xs);<br>let _ = useStr(xs);<br>// compiles cleanly; should fail because `xs` is value-restricted</pre> | n/a | Per `docs/spec/04-expressions/data-literals.md` §Empty List Type Inference (lines 380–421), `let xs = []` should be **monomorphic** — the value restriction prevents generalisation, so the first concrete use must fix the element type. The current implementation accepts `xs` at incompatible `List<Int>` and `List<String>` instantiations within the same module, indicating it is being treated as polymorphic across uses. The audit (`.claude/spec-audit/04b-expressions-data-fns.md`, F-07) describes the intended behavior and points at `infer-bindings.ts`'s `computeBindingScheme` as the locus. The `let-binding-helpers.ts` invariants (`infer-bindings-helpers.ts`) say `computeBindingScheme` should skip generalisation for non-`CoreVarPattern` and mutable bindings — but `[]` desugars to a `CoreVariant("Nil", [])`, which `isSyntacticValue` would classify as a value, allowing generalisation. The fix is likely to extend the value-restriction predicate so empty constructed values (or their type vars) are not generalised, mirroring the way `ref(x)` is handled. **Halt the PR for human triage** — soundness bug; touches generalisation logic shared by every binding path. Spec-validation coverage deferred to the fix PR (a placeholder comment marks the intended location at `tests/e2e/spec-validation/04-expressions.test.ts` under "empty list value restriction"); the assertion to add once the bug is fixed is `expectCompileError("let xs = []; let _: List<Int> = xs; let _: List<String> = xs;")`. | Open |
| VF-FC-0004 | `packages/core/src/typechecker/infer/infer-functions.ts` (suspected; lambda parameter inference for record patterns) | n/a — discovered while authoring fixed-input spec-validation tests for lambda destructuring (chunk 10, 04b F-16); not surfaced by a property | 3 | <pre>let f = ({ x, y }) =&gt; x + y;<br>let result = f({ x: 1, y: 2 });<br>// error[VF4500]: Cannot access field on non-record type type variable</pre> | n/a | Per `docs/spec/04-expressions/functions-composition.md` §Destructuring in Lambda Parameters (lines 53–71), the spec example `let getCoords = ({ x, y }) => (x, y);` uses no annotation — destructuring should be inferred from the body's field accesses (or, equivalently, the record pattern in the synthesised `match`). The desugarer at `curryLambda.ts:135–170` correctly lifts `({x,y}) => body` into `($tmp) => match $tmp { | {x,y} => body }`, but the typechecker rejects the match because `$tmp` is a fresh type variable rather than a record. With an explicit annotation (`({x,y}: {x: Int, y: Int}) => x + y`) the program compiles. Vibefun records are closed (no row polymorphism), but the typechecker should still be able to walk the record-pattern arm and produce the closed record type from the fields named in the pattern — this is what the spec's example assumes. Likely needs an inference rule that, on encountering a record pattern matched against a free type variable, instantiates the variable to the closed record type implied by the pattern's field set. **Halt the PR for human triage** — spec/implementation divergence; documented spec example fails to compile. Spec-validation coverage deferred to the fix PR (a placeholder comment marks the intended location at `tests/e2e/spec-validation/04-expressions.test.ts` under "lambda destructuring parameter"); the assertion to add once the bug is fixed is `expectRunOutput(withOutput("let f = ({ x, y }) => x + y; let result = f({ x: 1, y: 2 });", "String.fromInt(result)"), "3")`. | Open |
| VF-FC-0005 | `packages/core/src/parser/` (parser does not recognise `<ident> = <expr>` as a binding reassignment statement) | n/a — discovered while authoring fixed-input spec-validation tests for mutable binding reassignment (chunk 11, 07 F-14); not surfaced by a property | 3 | <pre>let mut x = ref(0);<br>x = ref(10);<br>// error[VF2107]: Expected ';' or newline between declarations</pre> | n/a | Per `docs/spec/07-mutable-references.md:54-82`, a `let mut x = ref(...)` binding may be reassigned to a new ref via `x = ref(...);` ("Less common, but allowed"). The audit (`07-mutable-references.md`, F-14) marked this as ✅ Implemented, but the parser does not accept the surface syntax — `=` after a previously-bound name is rejected as a stray declaration, both at the top level and inside blocks. Only `:=` (mutating the ref's contents) is accepted. The fix likely needs the parser/desugarer to admit `<ident> = <expr>` as an `Assign` node and the typechecker to enforce that the target is a `mut`-bound variable. The bug-pinning test in `tests/e2e/spec-validation/07-mutable-references.test.ts` documents the current `expectCompileError` behavior; once the bug is fixed flip it to `expectRunOutput(..., "10")` per the spec example. **Halt the PR for human triage** — spec/implementation divergence; the documented "Reassign the binding to a new ref" example fails to compile. | Open |
| VF-FC-0006 | `packages/core/src/codegen/es2020/` (the `panic` builtin has no codegen/runtime support; see `emit-expressions/variables.ts` and `runtime-helpers.ts`) | n/a — discovered while authoring the F-21 panic E2E test (chunk 12, 09 F-21); not surfaced by a property | 3 | <pre>let _crash = unsafe { panic("boom") };<br>// compiles cleanly, then at runtime:<br>// ReferenceError: panic is not defined</pre> | n/a | `panic` is registered as a builtin in the typechecker (`builtins.ts:126-127`, type `(String) -> Never`) and a program calling it **typechecks and compiles** (exit 0), but codegen emits a bare `panic("boom")` reference with no runtime definition: `emitVar` finds no External binding for `panic`, so it falls through to the regular-identifier path and emits the undefined global `panic`. At runtime the program dies with `ReferenceError: panic is not defined` instead of throwing the supplied message. Spec `docs/spec/09-error-handling.md:97-127` requires `panic` to throw a JS `Error` with the message and stop execution immediately. Fix: emit a panic runtime helper (e.g. `const $panic = (m) => { throw new Error(m); };`, gated by a usage flag like the `$intDiv`/`$intMod` helpers) and map the `panic` builtin to it (or register `panic` as an external with a `jsName`). Bug-pinned at `tests/e2e/spec-validation/09-error-handling.test.ts` (`[BUG: VF-FC-0006]`) asserting the current `panic is not defined` runtime error; once fixed, flip to `expectRuntimeError(withOutput('let _crash = unsafe { panic("boom") };', '"never"'), "boom")`. **Halt the PR for human triage** — a core language primitive (`panic`) is non-functional at runtime despite compiling. | Open |
| VF-FC-0007 | `packages/stdlib/src/result.ts:31`, `packages/stdlib/src/option.ts:31` (hardcoded `unwrap` panic messages diverge from spec) | n/a — discovered while authoring the F-07/F-13 unwrap-message tests (chunk 12, 09 F-07/F-13/F-24/F-26); not surfaced by a property | 2 | <pre>Result.unwrap(Err("failed"));<br>// throws: "Result.unwrap called on Err"<br>// spec:  "Called unwrap on Err value: failed"<br>Option.unwrap(None);<br>// throws: "Option.unwrap called on None"<br>// spec:  "Called unwrap on None"</pre> | n/a | The stdlib throws `"Result.unwrap called on Err"` / `"Option.unwrap called on None"`, but `docs/spec/11-stdlib/result.md:224` and `option.md:200` specify `"Called unwrap on Err value: failed"` (note: **embeds the error value**) and `"Called unwrap on None"`. The chunk-12 plan states the message "is part of the public API" and "the spec is the contract". Two divergences: (1) wording/order, (2) the spec's Result message interpolates the error value, which the impl omits entirely. Fix: align the thrown messages with the spec (threading the error value into the Result message) and update the bug-pinned assertions. Bug-pinned (exact current message, anchored regex) in `result.test.ts`/`option.test.ts` (`[BUG: VF-FC-0007]`); once aligned, flip the regexes to the spec wording. **Needs a human decision** on whether spec or impl wording is authoritative — in particular whether `unwrap` should leak the error value into its panic message. | Open |
| VF-FC-0008 | `packages/core/src/typechecker/environment.ts` (`groupDeclarationsByName`) + `typechecker.ts:80,325` | n/a — discovered while authoring fixed-input spec-validation tests for external overloading (chunk 13, 10 F-03/F-05/F-06/F-07); not surfaced by a property | 3 | <pre>external f: (Int) -> Int = "doIt";<br>external f: (Int, Int) -> Int = "other";<br>// compiles cleanly; spec requires VF4801 (inconsistent JS name)</pre> | n/a | External overload **validation** (VF4801 same jsName / VF4802 same `from` / VF4803 all-function shapes) and arity **resolution** are unreachable in the full pipeline. `buildEnvironment.groupDeclarationsByName` only groups declarations whose `kind` is `"ExternalDecl"`/`"ExternalBlock"`, but the desugarer rewrites those to `CoreExternalDecl` before the typechecker calls `buildEnvironment` (`typechecker.ts:80`, via a `module as unknown as Module` cast). No overload group is ever formed, so VF4801/4802/4803 never throw end-to-end, and the per-decl `CoreExternalDecl` handler (`typechecker.ts:325`) binds each external as `kind:"External"`, silently overwriting earlier same-named decls — a later call then fails arity unification (VF4021). `packages/core/src/typechecker/environment.test.ts` passes only because it calls `buildEnvironment` on the *pre-desugar* `Module`. Spec `docs/spec/10-javascript-interop/external-declarations.md:237-260`. Fix: group/validate on `CoreExternalDecl` in the post-desugar path, or move overload grouping ahead of desugaring. **Halt for human triage** — affects F-03 (resolution), F-05, F-06, F-07; V-layer tests deferred (placeholder in `tests/e2e/spec-validation/10-javascript-interop.test.ts`). | Open |
| VF-FC-0009 | `packages/core/src/typechecker/infer/infer-primitives.ts:396-400` (`convertTypeExpr` `CoreFunctionType`) | n/a — discovered while authoring fixed-input spec-validation tests for Promise/multi-arg externals (chunk 13, 10 F-20); not surfaced by a property | 3 | <pre>external add2: (Int, Int) -> Int = "((a,b)=>a+b)";<br>let r = unsafe { add2(3, 4) };<br>// error[VF4021]: Cannot unify functions with different arity: 2 vs 1<br>// also: let add: (Int,Int)->Int = (a,b)=>a+b;  // same VF4021</pre> | n/a | Every multi-parameter function **type** (external declarations *and* plain `let` annotations) fails to typecheck. `convertTypeExpr` lowers surface `(A,B)->R` to the uncurried `Fun([A,B],R)`, but the desugarer curries every lambda (`curryLambda`) and every application into nested single-arg `CoreApp`s (`desugarer.ts:190-205`). `inferApp` then unifies the callee against `Fun([oneArg],ret)`, and a 2-param `Fun` mismatches arity → VF4021. Spec `docs/spec/10-javascript-interop/external-declarations.md:296-305` states `(Int,Int)->Int` and `Int->Int->Int` are "the SAME type (auto-currying)". Fix: have `convertTypeExpr` curry `CoreFunctionType` with ≥2 params into nested single-param `Fun`s (or teach `inferApp`/`unify` to accept uncurried multi-param `Fun`). **Halt for human triage** — broad: blocks all multi-arg externals (incl. F-20 Promise `.then` chaining) and any multi-param function-type annotation. | Open |
| VF-FC-0010 | `packages/core/src/codegen/es2020/` (FFI return marshalling) + `packages/cli/src/` (`--runtime-checks` flag missing) | n/a — discovered while authoring fixed-input spec-validation tests for null→Option (chunk 13, 10 F-25); not surfaced by a property | 3 | <pre>external js_maybe: (Bool) -> Option&lt;Int&gt; = "(b) =&gt; b ? 5 : null";<br>match unsafe { js_maybe(false) } { \| Some(_) =&gt; "got" \| None =&gt; "none" }<br>// runtime: Error: Match exhausted (raw null reaches the match)</pre> | n/a | Spec `docs/spec/10-javascript-interop/type-safety.md:58-77` says a JS `null`/`undefined` returned from an external typed `-> Option<T>` is marshalled to `None` (value → `Some`). No conversion is emitted, so the raw `null` flows into the `match` and exhausts at runtime. The spec frames null-handling under runtime-check modes (`--runtime-checks=ffi\|all\|none`), but the CLI rejects `--runtime-checks` (`unknown option`) — the modes are unimplemented. **Needs human decision**: is null→None marshalling unconditional, or only under `--runtime-checks=ffi`? Bug-pinned at `tests/e2e/spec-validation/10-javascript-interop.test.ts` (`[BUG: VF-FC-0010]`, asserts current "Match exhausted"); once marshalling lands, flip to `expectRunOutput(..., "none")`. | Open |
| VF-FC-0011 | `packages/core/src/typechecker/unify.ts` (no top-type case for `Any`) | n/a — discovered while authoring fixed-input spec-validation tests for the Any opaque type (chunk 13, 10 F-22); not surfaced by a property | 3 | <pre>external to_any: (Int) -> Any = "((x)=>x)";<br>let x = unsafe { to_any(42) };<br>let y = x + 1;  // error[VF4020]: Cannot unify Any with Int</pre> | n/a | `Any` is represented as `Const "Any"` and `unify` has no special case, so it unifies only with itself — behaving as a rigid nominal type, not a top type. Spec `docs/spec/10-javascript-interop/external-declarations.md:104-126` is ambiguous: "completely unconstrained… Equivalent to TypeScript's any or unknown" implies free unification, but "Can only pass to externals that accept Any" implies opacity; documented examples only ever flow `Any→Any` (which works today). **Needs human decision**: should `Any` unify with arbitrary concrete types (top type) or stay opaque (Any↔Any only)? The Any↔Any pass-through has a passing V-layer test; the concrete direction is deferred (placeholder comment in `10-javascript-interop.test.ts`). | Open |
| VF-FC-0012 | `packages/core/src/parser/parse-expression-operators.ts` (postfix `parseCall` loop handles only LPAREN/DOT, no LBRACKET) + `packages/core/src/types/ast.ts` (no `Index` node) | n/a — discovered while authoring fixed-input spec-validation tests for the `[]` indexing operator (chunk 16, 13 F-33); not surfaced by a property | 3 | <pre>let xs = [10, 20, 30];<br>let y = xs[0];   // error[VF2107]: Expected ';' or newline<br>let r = { a: 1 };<br>let y2 = r["a"]; // error[VF2107]</pre> | n/a | Spec `docs/spec/13-appendix.md:89` lists `[]` as a precedence-16 list/record **indexing** operator. It is **not implemented**: there is no `Index` AST variant anywhere in `core/src/types/ast.ts`, and the postfix loop in `parse-expression-operators.ts` (`parseCall`, precedence 15) only consumes `LPAREN` (call) and `DOT` (record access) — never `LBRACKET`. So `xs[0]` / `r["a"]` die at the parser with `VF2107`. NB: the audit (`.claude/spec-audit/13-appendix.md` F-33) wrongly marks this ✅ Implemented citing an "`Index` node at `ast.ts:85`" (that line is the `List` node) and "parsePostfix handles LBRACKET" (no such code) — the audit entry must be corrected. Fix: add an `Index` surface node + an `LBRACKET` postfix parse rule, then thread it through desugar/typecheck/codegen. **Also needs a spec decision**: out-of-bounds list index and missing record key are currently undocumented (Option vs panic) — the appendix only gives precedence. Bug-pinned at `tests/e2e/spec-validation/04-expressions.test.ts` (`[BUG: VF-FC-0012]`, asserts current `VF2107`); once implemented, flip to read + out-of-bounds tests per the chosen semantics. **Halt the PR for human triage** — documented operator entirely unimplemented; needs spec + audit correction. | Open |

## Triage tiers (reference)

- **Tier 1 — Trivial:** off-by-one / missing guard / wrong identity. Fix < 20 LOC, one file. Fix in same commit as the property; add the shrunken counterexample as a fixed regression test alongside.
- **Tier 2 — Localized:** correctness bug confined to one module, no public API change. Skip + backlog in commit N; fix and re-enable in commit N+1, same PR. Remove the backlog entry on fix.
- **Tier 3 — Soundness/design:** typechecker accepts ill-typed program; optimizer changes observable semantics; codegen emits incorrect JS. Skip + backlog + **halt the PR** for human triage.
