---
name: continue-feature
description: Continue implementation of a feature.
argument-hint: <feature-name>
---

Carefully review the `$0` feature (`$0-plan.md`, `$0-context.md`, and `$0-tasks.md`), then continue implementing this feature. After completing each phase, validate with `pnpm run verify`, mark the completed tasks in the task list, commit, and then stop for review.
